<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"><%= t('brand_name')  %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to t('boards.index.new'), new_board_path %>
      </li>
    </ul>
    <ul class="nav justify-content-end">
      <% if signed_in? %>
        <li class="nav-item active">
          <%= link_to current_user.email, edit_user_path(current_user)  %>
        </li>
        <li class="nav-item">
          <%= link_to t('sessions.destroy.exit'),
                      session_path(current_user),
                      method: :delete,
                      class: 'btn btn-warning' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to t('sessions.new.button_login_label'),
                      new_session_path,
                      class: 'btn btn-success' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
