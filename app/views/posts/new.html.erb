<div class='container-fluid'>
  <div class='row'>
    <div class='col'></div>
    <div class='col-2'>
      <h2><%= t('.title') %></h2>
      <ul class='text-danger'>
        <% @post.errors.full_messages.each do |message| %>
          <li> <%= message %> </li>
        <% end %>
      </ul>
      <%= form_with model: @post, url: '#',class: 'form-horizontal', local: true do |f| %>
        <div class='form-group'>
          <%= f.label :title %>
          <div class=''>
            <%= f.text_field :title, required: true %>
          </div>
        </div>
        <div class='form-group'>
          <%= f.label :description %>
          <div class=''>
            <%= f.text_area :description %>
          </div>
        </div>
        <div class=''>
          <%= f.label :conslusion_deadline %>
          <div class=''>
            <%= f.datetime_local_field :conslusion_deadline %>
          </div>
        </div>
      <% end %>
    </div>
    <div class='col'></div>
  </div>
</div>
